That is a cool tool. The ads are HORRENDOUS, but being able to select an output, and this builds the plan for you is really cool9:23 AMRecognized user's appreciation for automated planning despite advertising frustrationRecognized user's appreciation for automated planning despite advertising frustrationYeah that network graph view is genuinely useful — you can see the full dependency chain at a glance. The reinforced iron plate one in your second screenshot is a good example of why the Reddit post's "break big into small" tip matters. That graph is showing you exactly the sub-factories you need: iron rods, screws, and iron plates all feeding into the assembler. Without something like that it's easy to start building the assembler and then realize you have nothing feeding it.
The ads are rough though. That's the tradeoff with free community tools — the developer is paying server costs out of pocket and slapping ads on it to cover it. The Barilla pasta ad appearing three times on a factory planning tool is peak internet.
Worth bookmarking satisfactory-calculator.com on desktop for when you're doing serious pre-planning before a session. Then use the notes app on your phone while you're actually playing. They serve different moments pretty well.That wouldn't necessarily be difficult to build out into an app would it? Since the math and all the prereqs for output is available on the wiki yeah?9:24 AMWeighed feasibility: data accessible, math simple, UI complexity remainsWeighed feasibility: data accessible, math simple, UI complexity remainsThe math itself is straightforward — it's just recursive multiplication. If you want 10 reinforced iron plates/min, you work backwards through each recipe multiplying the required rates. Nothing complex there.
The data is the bigger question. The wiki has everything but parsing wikitext for every item reliably is messy, as you've already seen with our wiki tab. The better path is that the game ships with a file called Docs.json that contains every recipe, ingredient, machine, and rate in clean structured JSON. The community has it extracted and hosted publicly. That's actually what satisfactory-calculator.com uses under the hood — several people have built npm packages around it already.
So the real work is three things. First, loading and indexing that Docs.json data. Second, writing the recursive graph traversal that walks from your desired output back through every ingredient to the raw ore. Third, rendering it — the network graph view is the hard part visually, but a tree list view like the tab on that site is much simpler and honestly more useful on mobile.
The ad-free mobile-friendly version of that tool with a clean UI is a legitimate gap. The network graph would take real work to do well, but a "tell me what I need to build X at Y/min, show me the machine counts and sub-factory list" view could be added to the notes app without too much pain. It would fit naturally as a sixth tab sitting next to the wiki lookup.
Worth doing if you want to take this further. It's a self-contained project with well-defined inputs and outputs, which makes it a good Claude Code candidate if you want to build it out properly.Here's the other three tabs9:31 AMAnalyzed three interface views to inform design decisionsAnalyzed three interface views to inform design decisionsThe tree list (image 1) is the one to build. That's all you need on mobile — hierarchical breakdown, machine counts, rates. Clean and scannable with one thumb.
The items tab (image 2) is just a flattened version of the same data, easy to derive once you have the tree.
The buildings tab (image 3) is interesting but that's a different data set — it's pulling construction costs for the machines themselves, not just the production chain. That's a nice-to-have, not essential.
The tree list view maps directly to what Docs.json gives you. You pick an output, set a target rate, and the recursive traversal builds exactly that tree — item name, machine type, machine count, rate at each node. On mobile that renders as an indented list, which is actually better than the network graph since you don't need to drag or zoom anything.
The scoped version for the notes app would be: type an item name, type a target rate, get the tree. No save files, no interactive map, no blueprints. Just the one thing that answers "what do I need to build this." That's a tight scope and probably a weekend project with Claude Code handling the Docs.json parsing and traversal logic.